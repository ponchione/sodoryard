# Task 03: Create Main Entry Point

**Epic:** 01 — Project Scaffolding
**Status:** ⬚ Not started
**Dependencies:** Task 01

---

## Description

Create `cmd/sirtopham/main.go` as the CLI entry point. It should compile, print a version string, and exit cleanly.

## Acceptance Criteria

- [ ] `cmd/sirtopham/main.go` exists with `package main` and a `main()` function
- [ ] Running the binary prints a version string to stdout
- [ ] The binary exits with code 0
- [ ] `go build ./cmd/sirtopham/` produces a working binary
